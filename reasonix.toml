# Reasonix configuration.
# Resolution order: flag > ./reasonix.toml > ~/.config/reasonix/config.toml > built-in defaults.
# Secrets come from the environment via api_key_env; never put keys here.

config_version = 3   # schema marker for diagnostics; old versions may ignore it
default_model = "deepseek-flash"
# language      = "zh"   # ui/model language; empty = auto-detect from $LANG / $REASONIX_LANG

[agent]
# system_prompt = """..."""   # omit to use the built-in prompt for this version
# system_prompt_file = "prompts/system.md"   # overrides system_prompt when set
max_steps         = 0   # executor tool-call rounds; 0 = no limit
planner_max_steps = 12   # planner read-only tool-call rounds; 0 = no limit
temperature       = 0.0
auto_plan   = "off"   # off|on; off keeps plan mode manual
# auto_plan_classifier = "deepseek-flash"   # optional; only used for borderline tasks
soft_compact_ratio  = 0.5   # notice only; keeps cache-first prefix intact
compact_ratio       = 0.8   # try compacting when prompt reaches this fraction
compact_force_ratio = 0.9   # force compacting at this high-water mark
# planner_model = "mimo"   # optional: enable two-model collaboration
# subagent_model = "deepseek-pro"   # optional default for runAs=subagent skills
# subagent_models = { review = "deepseek-pro", security_review = "deepseek-pro" }   # per-skill overrides
# subagent_effort = "high"   # optional default effort for subagents
# subagent_efforts = { review = "max", task = "high" }   # per-tool/skill effort overrides
# output_style = "explanatory"   # explanatory | learning | concise | custom; empty = default

[tools]
enabled = []   # empty = all built-in tools
bash_timeout_seconds = 120   # foreground safety cap; set 0 for no tool-local cap

[codegraph]
enabled      = true   # built-in MCP server; off by default for first-run sessions
auto_install = true   # fetch the runtime when CodeGraph is enabled but missing
# path       = ""   # empty = cache, then PATH, then a bundle beside reasonix

[lsp]
enabled = true   # language server tools; servers launch lazily when used
# [lsp.servers.go]
# command = "gopls"
# args = []
# extensions = [".go"]

[skills]
# paths = ["~/my-skills", "../shared/skills"]   # extra custom skill roots
# excluded_paths = ["~/.agents/skills"]   # hide convention roots without deleting folders
# max_depth = 3   # nested scan depth; set 1 for legacy root-only discovery
# disabled_skills = ["review"]   # hide noisy or unwanted skills

[permissions]
# Per-call gating. mode = writer fallback when no rule matches: ask|allow|deny.
# Readers always default to allow. Precedence: deny > ask > allow > fallback.
# Rules are "Tool" or "Tool(specifier)"; e.g. Bash(go test:*), Edit(src/**).
mode  = "ask"
# deny = ["Bash(rm -rf*)", "Bash(git push*)"]   # hard-blocked in every mode
allow = ["kill_shell", "Bash(Start-Sleep 30; Invoke-WebRequest -Uri http://localhost:3000 -UseBasicParsing -TimeoutSec 3 | Select-Object StatusCode; Invoke-WebRequest -Uri http://localhost:8080/api/gantt/x/tasks -UseBasicParsing -TimeoutSec 3 | Select-Object StatusCode)", "Bash($r1 = Invoke-WebRequest -Uri http://localhost:3000 -UseBasicParsing -TimeoutSec 3; $r2 = Invoke-WebRequest -Uri http://localhost:8080/api/gantt/x/tasks -UseBasicParsing -TimeoutSec 3; Write-Host \"Frontend: $($r1.StatusCode)\"; Write-Host \"Backend: $($r2.StatusCode)\")", "Bash(curl -s -o /dev/null -w \"%{http_code}\" http://localhost:3000; echo; curl -s -o /dev/null -w \"%{http_code}\" http://localhost:8080/api/gantt/x/tasks)", "Bash(docker start syson-postgres 2>$null; cmd /c \"set SPRING_DATASOURCE_URL=jdbc:postgresql://localhost:15432/postgres&set SPRING_DATASOURCE_USERNAME=username&set SPRING_DATASOURCE_PASSWORD=password&cd /d F:\\\\WorkSpace\\\\AIWorkSpace\\\\syson-ds\\\\backend\\\\application\\\\syson-application && mvn spring-boot:run -nsu -DskipTests -Dcheckstyle.skip=true -Dcyclonedx.skip=true 2>&1\")", "Bash(Get-Process -Name java,node -ErrorAction SilentlyContinue | Stop-Process -Force; docker start syson-postgres 2>$null; Start-Sleep 3)", "Bash(docker container ls 2>&1; echo \"---\"; docker container ls --all 2>&1)", "Bash(Get-Process \"Docker Desktop\" -ErrorAction SilentlyContinue | Select-Object Id,ProcessName)", "Bash(Get-Process -Name java -ErrorAction SilentlyContinue | Stop-Process -Force; Get-Process -Name node -ErrorAction SilentlyContinue | Stop-Process -Force)", "Bash(Get-Process -Name java,node -ErrorAction SilentlyContinue | Stop-Process -Force)", "Bash(tasklist | findstr \"java.exe node.exe\")", "Bash(Start-Process docker:*)", "Bash($env:SPRING_DATASOURCE_URL='jdbc:postgresql://localhost:5555/postgres'; $env:SPRING_DATASOURCE_USERNAME='username'; $env:SPRING_DATASOURCE_PASSWORD='password'; Start-Process cmd -ArgumentList '/k','cd /d F:\\WorkSpace\\AIWorkSpace\\syson-ds\\backend\\application\\syson-application && mvn -nsu spring-boot:run -Dspring-boot.run.jvmArguments=-Xmx2048m -Dcheckstyle.skip=true -Dcyclonedx.skip=true')", "Bash(tasklist | findstr \"java.exe\"; tasklist | findstr \"node.exe\")", "Bash(Get-Process java,node -ErrorAction SilentlyContinue | Stop-Process -Force)", "Bash(docker rm -f syson-postgres 2>$null; docker run -d --name syson-postgres -e POSTGRES_USER=username -e POSTGRES_PASSWORD=password -e POSTGRES_DB=postgres -p 5555:5432 postgres:15 2>$null; Start-Sleep 4)", "Bash(netstat -ano | findstr \":8080 :5173\")", "Bash(docker info 2>&1 | Select-Object -First 5)", "Bash(Get-Process -Name \"java\",\"node\" -ErrorAction SilentlyContinue | Select-Object Name,Id,StartTime)", "Bash(netstat -ano | Select-String \"5555\")", "Bash(docker start syson-postgres; \"Exit code: $LASTEXITCODE\")", "Bash(Get-Command docker | Select-Object Source)", "Bash(Write-Host \"hello\"; Get-Date)", "Bash(docker info 2>&1 | Select-Object -First 3)", "Bash(mvn --version 2>&1 | Select-Object -First 2)", "Bash($env:JAVA_HOME; $env:Path -split ';' | Select-String -Pattern 'java|maven|node' -CaseSensitive:$false)", "Bash(docker version 2>&1 | Out-String)", "Bash($env:SPRING_DATASOURCE_URL = \"jdbc:postgresql://localhost:5555/postgres\"; $env:SPRING_DATASOURCE_USERNAME = \"username\"; $env:SPRING_DATASOURCE_PASSWORD = \"password\"; Set-Location F:\\WorkSpace\\AIWorkSpace\\syson-ds\\backend\\application\\syson-application; mvn -nsu spring-boot:run -Dspring-boot.run.jvmArguments=-Xmx2048m -Dcheckstyle.skip=true -Dcyclonedx.skip=true 2>&1)", "Bash(Set-Location F:\\WorkSpace\\AIWorkSpace\\syson-ds\\frontend\\syson; npm start 2>&1)", "Bash(Test-Path \"E:\\SoftWare\\Maven3.8.4\\bin\\mvn.cmd\"; Test-Path \"E:\\SoftWare\\nodejs20\\nodejs\\npm.cmd\"; Test-Path \"E:\\SoftWare\\Java21\\bin\\java.exe\")", "Bash(\"test\" | Out-File -FilePath F:\\WorkSpace\\AIWorkSpace\\syson-ds\\_test_output.txt -Encoding utf8; Get-Content F:\\WorkSpace\\AIWorkSpace\\syson-ds\\_test_output.txt)", "Bash(Start-Sleep -Seconds 5; Get-Content F:\\WorkSpace\\AIWorkSpace\\syson-ds\\_backend.log -Tail 20 -ErrorAction SilentlyContinue)", "Bash(Get-Command mvn -ErrorAction SilentlyContinue | Select-Object -ExpandProperty Source; Get-Command npm -ErrorAction SilentlyContinue | Select-Object -ExpandProperty Source; Get-Command java -ErrorAction SilentlyContinue | Select-Object -ExpandProperty Source)", "Bash($proc = Start-Process -FilePath \"node\" -ArgumentList \"-e\",\"console.log('hi')\" -NoNewWindow -Wait -PassThru; $proc.ExitCode)", "Bash($env:SPRING_DATASOURCE_URL=\"jdbc:postgresql://localhost:5555/postgres\"; $env:SPRING_DATASOURCE_USERNAME=\"username\"; $env:SPRING_DATASOURCE_PASSWORD=\"password\"; Set-Location F:\\WorkSpace\\AIWorkSpace\\syson-ds\\backend\\application\\syson-application; mvn -nsu spring-boot:run -Dspring-boot.run.jvmArguments=-Xmx2048m -Dcheckstyle.skip=true -Dcyclonedx.skip=true)", "Bash(Set-Location F:\\WorkSpace\\AIWorkSpace\\syson-ds\\frontend\\syson; npm start)", "Bash(Test-Path \"E:\\SoftWare\\Maven3.8.4\\bin\\mvn.cmd\" 2>&1; Test-Path \"E:\\SoftWare\\nodejs20\\nodejs\\npm.cmd\" 2>&1; Test-Path \"E:\\SoftWare\\Java21\\bin\\java.exe\" 2>&1)", "Bash(Get-Process java,node -ErrorAction SilentlyContinue | Select-Object Id,ProcessName)", "Bash(where.exe docker 2>&1; \"C:\\Program Files\\Docker\\Docker\\resources\\bin\\docker.exe\" ps 2>&1)", "Bash(\"OK\" | Out-File -FilePath F:\\WorkSpace\\AIWorkSpace\\syson-ds\\_psout.txt)", "explore"]
# ask = ["Edit(src/**)"]   # force a prompt even if otherwise allowed

[sandbox]
# Confine tool blast radius. File-writers (write_file/edit_file/multi_edit)
# may only write under workspace_root (empty = current dir) + allow_write.
# bash = "enforce" (default) jails each command in an OS sandbox (macOS now;
# graceful fallback elsewhere); "off" disables it. network allows egress.
# workspace_root = ""            # default: current working directory
# allow_write = ["/tmp"]          # extra dirs writers may also modify
bash    = "enforce"
network = true

[statusline]
# A custom status line: a command whose first stdout line replaces the built-in
# data row. It receives {"model","contextUsed","contextWindow","cwd"} as JSON on stdin.
# command = "my-statusline.sh"

# External MCP servers. type: "stdio" (default, a subprocess) | "http" | "sse".
# ${VAR} / ${VAR:-default} are expanded from the environment in command/args/env/url/headers.
# [[plugins]]
# name    = "example"
# command = "reasonix-plugin-example"
# [[plugins]]                                  # a remote server over Streamable HTTP
# name    = "stripe"
# type    = "http"
# url     = "https://mcp.stripe.com"
# headers = { Authorization = "Bearer ${STRIPE_KEY}" }
